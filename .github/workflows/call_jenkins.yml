name: on pr

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  call-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -X POST "http://13.60.66.220:8080//job/GitClone/build?token=mytoken123"
