name: on pr

on:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  call-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins Job
        run: |
          curl -u Vignesh:1121201e9007d7026bf01877253aec6a13 http://13.60.66.220:8080//crumbIssuer/api/json
